# Conformance artifacts are byte-exact test inputs.
crates/yaml-sigil-conformance/fixtures/** -text

# Package-content inventories are canonical data consumed byte-for-byte by
# xtask.
xtask/package-contents/*.txt text eol=lf
